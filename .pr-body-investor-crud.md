## Summary
- Validação Bybit no vincular/atualizar usa timeout de **4s** em background; HTTP responde na hora com `status: warning`.
- Erros **33004/10003** (chave expirada/inválida) marcam o investidor como `erro_autenticacao` no SQLite e o monitor ignora a conta sem spam de logs.
- DELETE (`/api/cliente/{id}`, `/api/investidores/{id}`, `/api/deletar_investidor`) remove só no SQLite — **sem consultar Bybit**.
- Frontend: `AbortController` 6s no salvar/excluir + refresh imediato da lista.

## Test plan
- [x] `pytest tests/test_investor_crud_timeout.py` (5 passed)
- [ ] Vincular investidor com chave expirada → salva + warning, não timeout 15s
- [ ] Excluir investidor com erro_autenticacao → some da lista
- [ ] Logs do monitor não repetem 33004 a cada segundo
