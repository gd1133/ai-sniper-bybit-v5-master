## Summary
- Boot do robô (`preload` + sniper/monitores) moveu para thread daemon — Gunicorn libera HTTP imediatamente (anti 502/timeout no Render).
- `/api/status` responde do cache/memória e agenda refresh Bybit/PnL em background; novos `/api/health` e `/api/posicoes`.
- CORS irrestrito, no-cache nas rotas `/api/*`, error handler global com JSON 200 (sem 500 HTML no frontend).
- Frontend usa URLs relativas (`/api/...`), polling 3–4s + health check 15s; Gunicorn com 8 threads.

## Test plan
- [x] `pytest tests/test_dashboard_api_nonblocking.py`
- [ ] Deploy Render — dashboard abre sem loading infinito
- [ ] Conferir `/api/health` e `/api/status` no browser
- [ ] Cards de investidores/posições atualizam a cada poucos segundos
