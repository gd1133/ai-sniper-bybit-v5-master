services:
  - type: web
    name: ai-sniper-bybit-v5-mestre
    env: python
    plan: free
    envVars:
      - key: ENVIRONMENT
        value: development
    buildCommand: pip install -r requirements.txt && npm ci && npm run build
    startCommand: python -m gunicorn wsgi:app --config gunicorn.conf.py
